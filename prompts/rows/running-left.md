This row is deterministically derived from the approved running-right strip using a framewise horizontal mirror, preserving all eight frame positions and playback order. The visual reviewer confirmed no readable text or side-specific identity mark prevents mirroring. See production.json for the source and decision.
